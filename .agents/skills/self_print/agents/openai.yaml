interface:
  display_name: "Self Print"
  short_description: "Repo-specific patterns and workflows for self_print"
  default_prompt: "Use the self_print repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true