@echo off
REM Self-elevating: kills ONLY the SelfPrint agent running from this dev box.
REM Use this when the agent should run on the shop PC, not here.
powershell -NoProfile -ExecutionPolicy Bypass -Command "if (-not ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltinRole]::Administrator)) { Start-Process -FilePath '%~f0' -Verb RunAs; exit }; $killed=0; Get-CimInstance Win32_Process | Where-Object { $_.Name -in @('node.exe','cmd.exe') -and $_.CommandLine -match 'Selfprint' -and $_.CommandLine -match 'agent|index|tsx' } | ForEach-Object { try { Stop-Process -Id $_.ProcessId -Force -ErrorAction Stop; Write-Host ('Killed PID ' + $_.ProcessId); $script:killed++ } catch { Write-Host ('Could not kill ' + $_.ProcessId + ': ' + $_.Exception.Message) } }; if ($killed -eq 0) { Write-Host 'No SelfPrint agent process found (already stopped).' }; Write-Host ''; Write-Host 'Done. This dev box will no longer steal print jobs.'; Write-Host 'Press any key to close...'; $null = $host.UI.RawUI.ReadKey('NoEcho,IncludeKeyDown')"
